name: Two-way mirror (main)

on:
  push:
    branches: ["main"]
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:

concurrency:
  group: main-mirror
  cancel-in-progress: true

jobs:
  push_to_partner:
    if: github.event_name == 'push'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Use MIRROR_PAT for GitHub HTTPS
        run: |
          git config --global --unset-all http.https://github.com/.extraheader || true
          git config --global url."https://x-access-token:${{ secrets.MIRROR_PAT }}@github.com/".insteadOf "https://github.com/"

      - name: Push main to partner
        run: |
          git remote add partner https://github.com/navpass/navpass-website.git || true
          git fetch --prune partner +refs/heads/main:refs/remotes/partner/main
          # tenta push normal; se parceiro estiver na frente, falha (correto)
          git push partner main:main

  pull_from_partner:
    if: github.event_name != 'push'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Configure git identity + auth
        run: |
          git config user.name "mirror-bot"
          git config user.email "mirror-bot@users.noreply.github.com"
          git config --global --unset-all http.https://github.com/.extraheader || true
          git config --global url."https://x-access-token:${{ secrets.MIRROR_PAT }}@github.com/".insteadOf "https://github.com/"

      - name: Pull partner -> fast-forward/merge -> push origin
        run: |
          # garante que não tem credenciais do GITHUB_TOKEN atrapalhando
          git config --global --unset-all http.https://github.com/.extraheader || true

          echo "Remotes before:"
          git remote -v

          # recria o remote do partner JÁ com token (fetch e push vão funcionar)
          git remote remove partner || true
          git remote add partner https://x-access-token:${{ secrets.MIRROR_PAT }}@github.com/navpass/navpass-website.git

          # Atualiza refs de forma determinística
          git fetch --prune origin  +refs/heads/main:refs/remotes/origin/main
          git fetch --prune partner +refs/heads/main:refs/remotes/partner/main

          git checkout main
          git reset --hard refs/remotes/origin/main

          OUR="$(git rev-parse refs/remotes/origin/main)"
          THEIR="$(git rev-parse refs/remotes/partner/main)"
          BASE="$(git merge-base refs/remotes/origin/main refs/remotes/partner/main)"

          echo "origin/main:  $OUR"
          echo "partner/main: $THEIR"
          echo "merge-base:   $BASE"

          if [ "$OUR" = "$THEIR" ]; then
            echo "Already in sync."
            exit 0
          fi

          if [ "$THEIR" = "$BASE" ]; then
            echo "Partner is behind. No pull needed."
            exit 0
          fi

          if [ "$OUR" = "$BASE" ]; then
            echo "Fast-forwarding our main to partner/main..."
            git merge --ff-only refs/remotes/partner/main
          else
            echo "Diverged. Attempting merge..."
            git merge --no-edit refs/remotes/partner/main
          fi

          git push origin main
